import{Router}from'express';import{z}from'zod';import{db}from'../../config/db.js';import{requireAuthentication}from'../../middleware/authentication.js';import{requirePermission}from'../../middleware/authorization.js';export const muhafadlohImportRouter=Router();muhafadlohImportRouter.use(requireAuthentication,requirePermission('muhafadloh.import.manage'));const id=z.coerce.number().int().positive();const year=z.object({academicYearId:z.coerce.number().int().positive()});muhafadlohImportRouter.get('/:batchId/review',async(req,res,next)=>{try{const b=id.safeParse(req.params.batchId);if(!b.success){res.status(400).json({error:'INVALID_IMPORT_BATCH'});return}const[batch]:any=await db.query(`SELECT id,source_type,source_name,source_checksum,status,created_at,approved_at FROM import_batches WHERE id=? AND source_type='MUHAFADLOH' LIMIT 1`,[b.data]);if(!batch[0]){res.status(404).json({error:'IMPORT_BATCH_NOT_FOUND'});return}const[rows]=await db.query(`SELECT r.id,r.source_row_number,r.source_nis,r.source_name,r.source_class,r.m1,r.matched_student_id,r.match_status,r.review_note,s.nis official_nis,s.name official_name,s.status student_status FROM import_muhafadloh_rows r LEFT JOIN students s ON s.id=r.matched_student_id WHERE r.batch_id=? ORDER BY r.source_row_number`,[b.data]);res.json({batch:batch[0],rows,openExecution:'m1'})}catch(e){next(e)}});muhafadlohImportRouter.post('/:batchId/approve',async(req,res,next)=>{try{const b=id.safeParse(req.params.batchId);if(!b.success){res.status(400).json({error:'INVALID_IMPORT_BATCH'});return}const[batch]:any=await db.query(`SELECT status FROM import_batches WHERE id=? AND source_type='MUHAFADLOH' LIMIT 1`,[b.data]);if(!batch[0]){res.status(404).json({error:'IMPORT_BATCH_NOT_FOUND'});return}if(!['STAGED','REVIEW_REQUIRED'].includes(batch[0].status)){res.status(409).json({error:'IMPORT_BATCH_NOT_REVIEWABLE'});return}const[bad]:any=await db.query(`SELECT COUNT(*) n FROM import_muhafadloh_rows WHERE batch_id=? AND (matched_student_id IS NULL OR match_status IN('UNMATCHED','NAME_CLASS_REVIEW','AMBIGUOUS'))`,[b.data]);if(Number(bad[0].n)>0){res.status(409).json({error:'IMPORT_REVIEW_INCOMPLETE'});return}const[result]:any=await db.execute(`UPDATE import_batches SET status='APPROVED',approved_by=?,approved_at=NOW() WHERE id=? AND status IN('STAGED','REVIEW_REQUIRED')`,[req.auth!.userId,b.data]);if(result.affectedRows!==1){res.status(409).json({error:'IMPORT_BATCH_STATE_CHANGED'});return}res.status(204).end()}catch(e){next(e)}});muhafadlohImportRouter.post('/:batchId/apply',async(req,res,next)=>{const b=id.safeParse(req.params.batchId),y=year.safeParse(req.query);if(!b.success||!y.success){res.status(400).json({error:'INVALID_IMPORT_APPLY'});return}const conn=await db.getConnection();try{await conn.beginTransaction();const[batch]:any=await conn.query(`SELECT status FROM import_batches WHERE id=? AND source_type='MUHAFADLOH' FOR UPDATE`,[b.data]);if(!batch[0]){await conn.rollback();res.status(404).json({error:'IMPORT_BATCH_NOT_FOUND'});return}if(batch[0].status!=='APPROVED'){await conn.rollback();res.status(409).json({error:'IMPORT_BATCH_NOT_APPROVED'});return}const[rows]:any=await conn.query(`SELECT id,matched_student_id,m1 FROM import_muhafadloh_rows WHERE batch_id=? ORDER BY source_row_number FOR UPDATE`,[b.data]);if(!rows.length){await conn.rollback();res.status(409).json({error:'IMPORT_BATCH_EMPTY'});return}for(const r of rows){if(!r.matched_student_id)throw new Error('UNMATCHED_MUHAFADLOH_ROW');const[en]:any=await conn.query(`SELECT class_id FROM student_enrollments WHERE student_id=? AND academic_year_id=? AND status IN('ACTIVE','PROMOTED','GRADUATED','WITHDRAWN') LIMIT 1`,[r.matched_student_id,y.data.academicYearId]);if(!en[0])throw new Error('STUDENT_NOT_ENROLLED_FOR_MUHAFADLOH');await conn.execute(`INSERT INTO muhafadloh(student_id,class_id,academic_year_id,m1,m1_import_row_id,m2,m3,m4,m5,m6,m7,m8,recorded_by) VALUES(?,?,?,?,?,NULL,NULL,NULL,NULL,NULL,NULL,NULL,?) ON DUPLICATE KEY UPDATE class_id=VALUES(class_id),m1=VALUES(m1),m1_import_row_id=VALUES(m1_import_row_id),recorded_by=VALUES(recorded_by)`,[r.matched_student_id,en[0].class_id,y.data.academicYearId,r.m1,r.id,req.auth!.userId])}const[result]:any=await conn.execute(`UPDATE import_batches SET status='IMPORTED' WHERE id=? AND status='APPROVED'`,[b.data]);if(result.affectedRows!==1)throw new Error('IMPORT_BATCH_STATE_CHANGED');await conn.commit();res.status(204).end()}catch(e:any){await conn.rollback();if(['UNMATCHED_MUHAFADLOH_ROW','STUDENT_NOT_ENROLLED_FOR_MUHAFADLOH','IMPORT_BATCH_STATE_CHANGED'].includes(e?.message)){res.status(409).json({error:e.message});return}next(e)}finally{conn.release()}});
