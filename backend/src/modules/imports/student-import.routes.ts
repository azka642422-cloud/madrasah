import{Router}from'express';import{z}from'zod';import{db}from'../../config/db.js';import{requireAuthentication}from'../../middleware/authentication.js';import{requirePermission}from'../../middleware/authorization.js';export const studentImportRouter=Router();studentImportRouter.use(requireAuthentication,requirePermission('students.import.manage'));const id=z.coerce.number().int().positive();studentImportRouter.get('/:batchId/review',async(req,res,next)=>{try{const b=id.safeParse(req.params.batchId);if(!b.success){res.status(400).json({error:'INVALID_IMPORT_BATCH'});return}const[batch]:any=await db.query(`SELECT id,source_type,source_name,source_checksum,status,created_at,approved_at FROM import_batches WHERE id=? AND source_type='WORD_STUDENT_MASTER' LIMIT 1`,[b.data]);if(!batch[0]){res.status(404).json({error:'IMPORT_BATCH_NOT_FOUND'});return}const[rows]=await db.query(`SELECT r.id,r.source_row_number,r.source_nis,r.source_name,r.source_class,r.birth_place,r.birth_date,r.matched_student_id,r.match_status,r.review_note,c.id matched_class_id,c.code matched_class_code FROM import_student_rows r LEFT JOIN classes c ON UPPER(TRIM(c.code))=UPPER(TRIM(r.source_class)) AND c.active=1 WHERE r.batch_id=? ORDER BY r.source_row_number`,[b.data]);res.json({batch:batch[0],rows})}catch(e){next(e)}});studentImportRouter.post('/:batchId/approve',async(req,res,next)=>{try{const b=id.safeParse(req.params.batchId);if(!b.success){res.status(400).json({error:'INVALID_IMPORT_BATCH'});return}const[batch]:any=await db.query(`SELECT status FROM import_batches WHERE id=? AND source_type='WORD_STUDENT_MASTER' LIMIT 1`,[b.data]);if(!batch[0]){res.status(404).json({error:'IMPORT_BATCH_NOT_FOUND'});return}if(!['STAGED','REVIEW_REQUIRED'].includes(batch[0].status)){res.status(409).json({error:'IMPORT_BATCH_NOT_REVIEWABLE'});return}const[bad]:any=await db.query(`SELECT COUNT(*) n FROM import_student_rows r WHERE r.batch_id=? AND (r.source_nis IS NULL OR TRIM(r.source_nis)='' OR TRIM(r.source_name)='' OR r.source_class IS NULL OR TRIM(r.source_class)='' OR r.match_status IN('AMBIGUOUS','EXACT_NAME_REVIEW') OR (SELECT COUNT(*) FROM classes c WHERE UPPER(TRIM(c.code))=UPPER(TRIM(r.source_class)) AND c.active=1)<>1)`,[b.data]);if(Number(bad[0].n)>0){res.status(409).json({error:'IMPORT_REVIEW_INCOMPLETE'});return}const[dup]:any=await db.query(`SELECT source_nis FROM import_student_rows WHERE batch_id=? GROUP BY source_nis HAVING COUNT(*)>1 LIMIT 1`,[b.data]);if(dup[0]){res.status(409).json({error:'DUPLICATE_OFFICIAL_NIS_IN_BATCH'});return}const[result]:any=await db.execute(`UPDATE import_batches SET status='APPROVED',approved_by=?,approved_at=NOW() WHERE id=? AND status IN('STAGED','REVIEW_REQUIRED')`,[req.auth!.userId,b.data]);if(result.affectedRows!==1){res.status(409).json({error:'IMPORT_BATCH_STATE_CHANGED'});return}res.status(204).end()}catch(e){next(e)}});studentImportRouter.post('/:batchId/apply',async(req,res,next)=>{const b=id.safeParse(req.params.batchId);if(!b.success){res.status(400).json({error:'INVALID_IMPORT_BATCH'});return}const conn=await db.getConnection();try{await conn.beginTransaction();const[batch]:any=await conn.query(`SELECT id,status FROM import_batches WHERE id=? AND source_type='WORD_STUDENT_MASTER' FOR UPDATE`,[b.data]);if(!batch[0]){await conn.rollback();res.status(404).json({error:'IMPORT_BATCH_NOT_FOUND'});return}if(batch[0].status!=='APPROVED'){await conn.rollback();res.status(409).json({error:'IMPORT_BATCH_NOT_APPROVED'});return}const[years]:any=await conn.query(`SELECT id FROM academic_years WHERE is_active=1 FOR UPDATE`);if(years.length!==1)throw new Error('EXACTLY_ONE_ACTIVE_ACADEMIC_YEAR_REQUIRED');const academicYearId=years[0].id;const[rows]:any=await conn.query(`SELECT id,source_nis,source_name,source_class,birth_place,birth_date,matched_student_id FROM import_student_rows WHERE batch_id=? ORDER BY source_row_number FOR UPDATE`,[b.data]);if(!rows.length){await conn.rollback();res.status(409).json({error:'IMPORT_BATCH_EMPTY'});return}for(const r of rows){const nis=String(r.source_nis??'').trim(),name=String(r.source_name??'').trim(),sourceClass=String(r.source_class??'').trim();if(!nis||!name||!sourceClass)throw new Error('INVALID_OFFICIAL_WORD_ROW');const[classes]:any=await conn.query(`SELECT id FROM classes WHERE UPPER(TRIM(code))=UPPER(?) AND active=1`,[sourceClass]);if(classes.length!==1)throw new Error('OFFICIAL_CLASS_REQUIRES_REVIEW');const classId=classes[0].id;let studentId=r.matched_student_id;if(studentId){const[s]:any=await conn.query(`SELECT id,nis,status FROM students WHERE id=? FOR UPDATE`,[studentId]);if(!s[0])throw new Error('MATCHED_STUDENT_NOT_FOUND');if(s[0].status!=='ACTIVE')throw new Error('STUDENT_REACTIVATION_REQUIRES_REVIEW');const[nisOwner]:any=await conn.query(`SELECT id FROM students WHERE nis=? AND id<>? LIMIT 1 FOR UPDATE`,[nis,studentId]);if(nisOwner[0])throw new Error('OFFICIAL_NIS_CONFLICT');await conn.execute(`UPDATE students SET nis=?,name=?,birth_place=?,birth_date=?,source_reference='WORD_STUDENT_MASTER',official_source_batch_id=? WHERE id=?`,[nis,name,r.birth_place??null,r.birth_date??null,b.data,studentId])}else{const[existing]:any=await conn.query(`SELECT id,status FROM students WHERE nis=? FOR UPDATE`,[nis]);if(existing[0]){studentId=existing[0].id;if(existing[0].status!=='ACTIVE')throw new Error('STUDENT_REACTIVATION_REQUIRES_REVIEW');await conn.execute(`UPDATE students SET name=?,birth_place=?,birth_date=?,source_reference='WORD_STUDENT_MASTER',official_source_batch_id=? WHERE id=?`,[name,r.birth_place??null,r.birth_date??null,b.data,studentId])}else{const[x]:any=await conn.execute(`INSERT INTO students(nis,name,birth_place,birth_date,status,source_reference,official_source_batch_id) VALUES(?,?,?,?,'ACTIVE','WORD_STUDENT_MASTER',?)`,[nis,name,r.birth_place??null,r.birth_date??null,b.data]);studentId=x.insertId}}const[enrollments]:any=await conn.query(`SELECT id,class_id,status,ended_on FROM student_enrollments WHERE student_id=? AND academic_year_id=? FOR UPDATE`,[studentId,academicYearId]);const enrollment=enrollments[0];if(enrollment){if(enrollment.status!=='ACTIVE')throw new Error('ENROLLMENT_STATUS_REQUIRES_REVIEW');if(enrollment.ended_on!=null)throw new Error('ENDED_ACTIVE_ENROLLMENT_REQUIRES_REVIEW');if(enrollment.class_id!==classId)throw new Error('ACTIVE_ENROLLMENT_CLASS_CONFLICT')}else{await conn.execute(`INSERT INTO student_enrollments(student_id,class_id,academic_year_id,status,started_on,ended_on) VALUES(?,?,?,'ACTIVE',NULL,NULL)`,[studentId,classId,academicYearId])}await conn.execute(`UPDATE import_student_rows SET matched_student_id=?,match_status='APPROVED' WHERE id=?`,[studentId,r.id])}const[result]:any=await conn.execute(`UPDATE import_batches SET status='IMPORTED' WHERE id=? AND status='APPROVED'`,[b.data]);if(result.affectedRows!==1)throw new Error('IMPORT_BATCH_STATE_CHANGED');await conn.commit();res.status(204).end()}catch(e:any){await conn.rollback();if(['INVALID_OFFICIAL_WORD_ROW','MATCHED_STUDENT_NOT_FOUND','OFFICIAL_NIS_CONFLICT','OFFICIAL_CLASS_REQUIRES_REVIEW','EXACTLY_ONE_ACTIVE_ACADEMIC_YEAR_REQUIRED','ACTIVE_ENROLLMENT_CLASS_CONFLICT','IMPORT_BATCH_STATE_CHANGED','STUDENT_REACTIVATION_REQUIRES_REVIEW','ENROLLMENT_STATUS_REQUIRES_REVIEW','ENDED_ACTIVE_ENROLLMENT_REQUIRES_REVIEW'].includes(e?.message)){res.status(409).json({error:e.message});return}if(e?.code==='ER_DUP_ENTRY'){res.status(409).json({error:'OFFICIAL_NIS_OR_ENROLLMENT_CONFLICT'});return}next(e)}finally{conn.release()}});
