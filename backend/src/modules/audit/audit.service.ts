import type{Request}from'express';import{db}from'../../config/db.js';export type AuditInput={userId?:number|null;action:string;entityType?:string;entityId?:string|number;metadata?:Record<string,unknown>|null};export async function writeAudit(req:Request,input:AuditInput){const forwarded=req.get('x-forwarded-for')?.split(',')[0]?.trim(),ip=(forwarded||req.ip||'').slice(0,45),ua=(req.get('user-agent')||'').slice(0,500);await db.execute(`INSERT INTO audit_logs(user_id,action,entity_type,entity_id,metadata,ip_address,user_agent) VALUES(?,?,?,?,?,?,?)`,[input.userId??req.auth?.userId??null,input.action,input.entityType??null,input.entityId==null?null:String(input.entityId),input.metadata?JSON.stringify(input.metadata):null,ip||null,ua||null])}
