import{Router}from'express';import{z}from'zod';import{db}from'../../config/db.js';import{requireAuthentication}from'../../middleware/authentication.js';import{requirePermission,requireRole}from'../../middleware/authorization.js';export const scheduleRouter=Router();scheduleRouter.use(requireAuthentication);scheduleRouter.get('/admin',requirePermission('schedules.manage'),async(_req,res,next)=>{try{const[rows]=await db.query(`SELECT sc.id,sc.day_of_week,sc.starts_at,sc.ends_at,sc.room_name,sc.active,ta.id AS assignment_id,t.id AS teacher_id,t.name AS teacher_name,c.id AS class_id,c.code AS class_code,su.id AS subject_id,su.name AS subject_name,su.book_name,ay.id AS academic_year_id,ay.name AS academic_year,ta.semester FROM schedules sc JOIN teaching_assignments ta ON ta.id=sc.teaching_assignment_id JOIN teachers t ON t.id=ta.teacher_id JOIN classes c ON c.id=ta.class_id JOIN subjects su ON su.id=ta.subject_id JOIN academic_years ay ON ay.id=ta.academic_year_id WHERE sc.active=1 ORDER BY FIELD(sc.day_of_week,'SENIN','SELASA','RABU','KAMIS','JUMAT','SABTU','MINGGU'),sc.starts_at,c.code`);res.json({schedules:rows})}catch(e){next(e)}});scheduleRouter.get('/teacher/me',requireRole('GURU'),async(req,res,next)=>{try{if(!req.auth!.teacherId){res.status(403).json({error:'TEACHER_PROFILE_REQUIRED'});return}const[rows]=await db.query(`SELECT sc.id,sc.day_of_week,sc.starts_at,sc.ends_at,sc.room_name,c.code AS class_code,su.name AS subject_name,su.book_name,ay.name AS academic_year,ta.semester FROM schedules sc JOIN teaching_assignments ta ON ta.id=sc.teaching_assignment_id JOIN classes c ON c.id=ta.class_id JOIN subjects su ON su.id=ta.subject_id JOIN academic_years ay ON ay.id=ta.academic_year_id WHERE ta.teacher_id=? AND ta.active=1 AND sc.active=1 AND ay.is_active=1 ORDER BY FIELD(sc.day_of_week,'SENIN','SELASA','RABU','KAMIS','JUMAT','SABTU','MINGGU'),sc.starts_at,c.code`,[req.auth!.teacherId]);res.json({schedules:rows})}catch(e){next(e)}});scheduleRouter.get('/student/me',requireRole('SANTRI'),async(req,res,next)=>{try{if(!req.auth!.studentId){res.status(403).json({error:'STUDENT_PROFILE_REQUIRED'});return}const[rows]=await db.query(`SELECT sc.id,sc.day_of_week,sc.starts_at,sc.ends_at,sc.room_name,t.name AS teacher_name,c.code AS class_code,su.name AS subject_name,su.book_name,ay.name AS academic_year,ta.semester FROM student_enrollments se JOIN academic_years ay ON ay.id=se.academic_year_id AND ay.is_active=1 JOIN teaching_assignments ta ON ta.class_id=se.class_id AND ta.academic_year_id=se.academic_year_id AND ta.active=1 JOIN schedules sc ON sc.teaching_assignment_id=ta.id AND sc.active=1 JOIN teachers t ON t.id=ta.teacher_id JOIN classes c ON c.id=se.class_id JOIN subjects su ON su.id=ta.subject_id WHERE se.student_id=? AND se.status='ACTIVE' ORDER BY FIELD(sc.day_of_week,'SENIN','SELASA','RABU','KAMIS','JUMAT','SABTU','MINGGU'),sc.starts_at`,[req.auth!.studentId]);res.json({schedules:rows})}catch(e){next(e)}});const base=z.object({teachingAssignmentId:z.number().int().positive(),dayOfWeek:z.enum(['SENIN','SELASA','RABU','KAMIS','JUMAT','SABTU','MINGGU']),startsAt:z.string().regex(/^([01]\d|2[0-3]):[0-5]\d$/),endsAt:z.string().regex(/^([01]\d|2[0-3]):[0-5]\d$/),roomName:z.string().trim().max(100).nullable().optional()});async function validate(v:any,exclude=0){if(v.endsAt<=v.startsAt)return'INVALID_TIME_RANGE';const[a]:any=await db.query(`SELECT id,teacher_id,class_id,academic_year_id,semester FROM teaching_assignments WHERE id=? AND active=1 LIMIT 1`,[v.teachingAssignmentId]);if(!a[0])return'INVALID_TEACHING_ASSIGNMENT';const[c]:any=await db.query(`SELECT sc.id FROM schedules sc JOIN teaching_assignments ta ON ta.id=sc.teaching_assignment_id WHERE sc.active=1 AND sc.id<>? AND sc.day_of_week=? AND sc.starts_at<? AND sc.ends_at>? AND ta.academic_year_id=? AND ta.semester=? AND (ta.teacher_id=? OR ta.class_id=?) LIMIT 1`,[exclude,v.dayOfWeek,v.endsAt,v.startsAt,a[0].academic_year_id,a[0].semester,a[0].teacher_id,a[0].class_id]);return c[0]?'SCHEDULE_CONFLICT':null}scheduleRouter.post('/',requirePermission('schedules.manage'),async(req,res,next)=>{try{const p=base.safeParse(req.body);if(!p.success){res.status(400).json({error:'INVALID_SCHEDULE'});return}const err=await validate(p.data);if(err){res.status(409).json({error:err});return}const[r]:any=await db.execute(`INSERT INTO schedules(teaching_assignment_id,day_of_week,starts_at,ends_at,room_name) VALUES(?,?,?,?,?)`,[p.data.teachingAssignmentId,p.data.dayOfWeek,p.data.startsAt,p.data.endsAt,p.data.roomName??null]);res.status(201).json({id:r.insertId})}catch(e){next(e)}});scheduleRouter.patch('/:scheduleId',requirePermission('schedules.manage'),async(req,res,next)=>{try{const id=Number(req.params.scheduleId);if(!Number.isSafeInteger(id)||id<=0){res.status(400).json({error:'INVALID_SCHEDULE_ID'});return}const[cur]:any=await db.query(`SELECT teaching_assignment_id AS teachingAssignmentId,day_of_week AS dayOfWeek,starts_at AS startsAt,ends_at AS endsAt,room_name AS roomName FROM schedules WHERE id=? AND active=1 LIMIT 1`,[id]);if(!cur[0]){res.status(404).json({error:'SCHEDULE_NOT_FOUND'});return}const p=base.partial().safeParse(req.body);if(!p.success||!Object.keys(p.data).length){res.status(400).json({error:'INVALID_SCHEDULE'});return}const merged={...cur[0],...p.data};const err=await validate(merged,id);if(err){res.status(409).json({error:err});return}await db.execute(`UPDATE schedules SET teaching_assignment_id=?,day_of_week=?,starts_at=?,ends_at=?,room_name=? WHERE id=?`,[merged.teachingAssignmentId,merged.dayOfWeek,merged.startsAt,merged.endsAt,merged.roomName??null,id]);res.status(204).end()}catch(e){next(e)}});scheduleRouter.delete('/:scheduleId',requirePermission('schedules.manage'),async(req,res,next)=>{try{const id=Number(req.params.scheduleId);const[r]:any=await db.execute(`UPDATE schedules SET active=0 WHERE id=? AND active=1`,[id]);if(!r.affectedRows){res.status(404).json({error:'SCHEDULE_NOT_FOUND'});return}res.status(204).end()}catch(e){next(e)}});
