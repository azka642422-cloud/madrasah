import{Router}from'express';import{z}from'zod';import{db}from'../../config/db.js';import{requireAuthentication}from'../../middleware/authentication.js';import{requirePermission,requireRole}from'../../middleware/authorization.js';export const certificateRouter=Router();certificateRouter.use(requireAuthentication);certificateRouter.get('/student/me',requireRole('SANTRI'),async(req,res,next)=>{try{if(!req.auth!.studentId){res.status(403).json({error:'STUDENT_PROFILE_REQUIRED'});return}const[rows]=await db.query(`SELECT id,certificate_number,issue_place,issue_date,hijri_issue_date,graduation_status,status,snapshot_json,issued_at FROM certificates WHERE student_id=? AND status='ISSUED' ORDER BY issued_at DESC`,[req.auth!.studentId]);res.json({certificates:rows})}catch(e){next(e)}});const schema=z.object({studentId:z.number().int().positive(),academicYearId:z.number().int().positive(),graduationStatus:z.enum(['PENDING','PASSED','NOT_PASSED']),certificateNumber:z.string().trim().min(1).max(100).nullable().optional(),issuePlace:z.string().trim().max(150).nullable().optional(),issueDate:z.string().date().nullable().optional(),hijriIssueDate:z.string().trim().max(100).nullable().optional()});async function snapshot(studentId:number,yearId:number,p:any){const[s]:any=await db.query(`SELECT s.id,s.nis,s.name,s.birth_place,s.birth_date,s.status AS student_status,se.status AS enrollment_status,se.class_id,c.code AS class_code,c.level,ay.name AS academic_year FROM students s JOIN student_enrollments se ON se.student_id=s.id AND se.academic_year_id=? JOIN classes c ON c.id=se.class_id JOIN academic_years ay ON ay.id=se.academic_year_id WHERE s.id=? LIMIT 1`,[yearId,studentId]);if(!s[0])return null;return{version:1,student:s[0],graduationStatus:p.graduationStatus,certificateNumber:p.certificateNumber??null,issuePlace:p.issuePlace??null,issueDate:p.issueDate??null,hijriIssueDate:p.hijriIssueDate??null}}certificateRouter.post('/draft',requirePermission('certificates.manage'),async(req,res,next)=>{try{const x=schema.safeParse(req.body);if(!x.success){res.status(400).json({error:'INVALID_CERTIFICATE'});return}const p=x.data,snap=await snapshot(p.studentId,p.academicYearId,p);if(!snap){res.status(400).json({error:'STUDENT_ENROLLMENT_NOT_FOUND'});return}const[e]:any=await db.query(`SELECT id,status FROM certificates WHERE student_id=? AND academic_year_id=? LIMIT 1`,[p.studentId,p.academicYearId]);if(e[0]?.status==='ISSUED'){res.status(409).json({error:'ISSUED_CERTIFICATE_IMMUTABLE'});return}if(e[0]?.status==='VOID'){res.status(409).json({error:'VOID_CERTIFICATE_IMMUTABLE'});return}if(e[0]){await db.execute(`UPDATE certificates SET certificate_number=?,issue_place=?,issue_date=?,hijri_issue_date=?,graduation_status=?,snapshot_json=?,updated_by=? WHERE id=? AND status='DRAFT'`,[p.certificateNumber??null,p.issuePlace??null,p.issueDate??null,p.hijriIssueDate??null,p.graduationStatus,JSON.stringify(snap),req.auth!.userId,e[0].id]);res.json({id:e[0].id});return}const[r]:any=await db.execute(`INSERT INTO certificates(student_id,academic_year_id,certificate_number,issue_place,issue_date,hijri_issue_date,graduation_status,status,snapshot_json,created_by,updated_by) VALUES(?,?,?,?,?,?,?,'DRAFT',?,?,?)`,[p.studentId,p.academicYearId,p.certificateNumber??null,p.issuePlace??null,p.issueDate??null,p.hijriIssueDate??null,p.graduationStatus,JSON.stringify(snap),req.auth!.userId,req.auth!.userId]);res.status(201).json({id:r.insertId})}catch(e:any){if(e?.code==='ER_DUP_ENTRY'){res.status(409).json({error:'CERTIFICATE_NUMBER_ALREADY_USED'});return}next(e)}});certificateRouter.post('/:certificateId/issue',requirePermission('certificates.manage'),async(req,res,next)=>{try{const id=Number(req.params.certificateId);if(!Number.isSafeInteger(id)||id<=0){res.status(400).json({error:'INVALID_CERTIFICATE_ID'});return}const[rows]:any=await db.query(`SELECT c.id,c.student_id,c.academic_year_id,c.status,c.graduation_status,c.certificate_number,c.issue_place,c.issue_date,c.hijri_issue_date,se.status AS enrollment_status,cl.level FROM certificates c JOIN student_enrollments se ON se.student_id=c.student_id AND se.academic_year_id=c.academic_year_id JOIN classes cl ON cl.id=se.class_id WHERE c.id=? LIMIT 1`,[id]),cert=rows[0];if(!cert){res.status(404).json({error:'CERTIFICATE_NOT_FOUND'});return}if(cert.status!=='DRAFT'){res.status(409).json({error:'CERTIFICATE_NOT_DRAFT'});return}if(cert.graduation_status!=='PASSED'){res.status(409).json({error:'GRADUATION_NOT_PASSED'});return}if(cert.level!==6){res.status(409).json({error:'FINAL_CLASS_REQUIRED'});return}if(!['ACTIVE','GRADUATED'].includes(cert.enrollment_status)){res.status(409).json({error:'ENROLLMENT_NOT_ELIGIBLE'});return}if(!cert.certificate_number||!cert.issue_date){res.status(409).json({error:'CERTIFICATE_IDENTITY_INCOMPLETE'});return}const snap=await snapshot(cert.student_id,cert.academic_year_id,{graduationStatus:cert.graduation_status,certificateNumber:cert.certificate_number,issuePlace:cert.issue_place,issueDate:String(cert.issue_date).slice(0,10),hijriIssueDate:cert.hijri_issue_date});const[u]:any=await db.execute(`UPDATE certificates SET status='ISSUED',snapshot_json=?,issued_at=NOW(),issued_by=?,updated_by=? WHERE id=? AND status='DRAFT'`,[JSON.stringify(snap),req.auth!.userId,req.auth!.userId,id]);if(!u.affectedRows){res.status(409).json({error:'CERTIFICATE_STATE_CHANGED'});return}res.status(204).end()}catch(e){next(e)}});certificateRouter.post('/:certificateId/void',requirePermission('certificates.manage'),async(req,res,next)=>{try{const id=Number(req.params.certificateId);if(!Number.isSafeInteger(id)||id<=0){res.status(400).json({error:'INVALID_CERTIFICATE_ID'});return}const[r]:any=await db.execute(`UPDATE certificates SET status='VOID',voided_at=NOW(),voided_by=?,updated_by=? WHERE id=? AND status IN('DRAFT','ISSUED')`,[req.auth!.userId,req.auth!.userId,id]);if(!r.affectedRows){res.status(409).json({error:'CERTIFICATE_NOT_VOIDABLE'});return}res.status(204).end()}catch(e){next(e)}});
