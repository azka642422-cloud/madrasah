import{Router}from'express';import multer from'multer';import crypto from'node:crypto';import path from'node:path';import fs from'node:fs/promises';import{fileURLToPath}from'node:url';import{db}from'../../config/db.js';import{requireAuthentication}from'../../middleware/authentication.js';import{requirePermission}from'../../middleware/authorization.js';const HERE=path.dirname(fileURLToPath(import.meta.url)),ROOT=process.env.UPLOAD_ROOT?path.resolve(process.env.UPLOAD_ROOT):path.resolve(HERE,'../../../../storage/uploads'),PHOTO_DIR=path.join(ROOT,'photos'),DOC_DIR=path.join(ROOT,'documents');const imageTypes=new Set(['image/jpeg','image/png','image/webp']),documentTypes=new Set(['application/pdf','application/vnd.openxmlformats-officedocument.wordprocessingml.document','application/vnd.openxmlformats-officedocument.spreadsheetml.sheet']);const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:10*1024*1024,files:1}});export const storageRouter=Router();storageRouter.use(requireAuthentication);function ext(m:string){return({'image/jpeg':'.jpg','image/png':'.png','image/webp':'.webp','application/pdf':'.pdf','application/vnd.openxmlformats-officedocument.wordprocessingml.document':'.docx','application/vnd.openxmlformats-officedocument.spreadsheetml.sheet':'.xlsx'}as Record<string,string>)[m]}function signature(f:Express.Multer.File){const b=f.buffer,m=f.mimetype;if(m==='image/jpeg')return b[0]===0xff&&b[1]===0xd8&&b[2]===0xff;if(m==='image/png')return b.subarray(0,8).equals(Buffer.from([137,80,78,71,13,10,26,10]));if(m==='image/webp')return b.subarray(0,4).toString()==='RIFF'&&b.subarray(8,12).toString()==='WEBP';if(m==='application/pdf')return b.subarray(0,5).toString()==='%PDF-';return b[0]===0x50&&b[1]===0x4b}async function save(dir:string,f:Express.Multer.File){await fs.mkdir(dir,{recursive:true});const n=crypto.randomUUID()+ext(f.mimetype);await fs.writeFile(path.join(dir,n),f.buffer,{flag:'wx'});return n}function full(rel:string){const p=path.resolve(ROOT,rel);return p.startsWith(ROOT+path.sep)?p:null}function sendFileStatus(e:Error&{statusCode?:number}){return e.statusCode===404?404:500}storageRouter.post('/students/:studentId/photo',requirePermission('students.manage'),upload.single('file'),async(req,res,next)=>{let saved:string|undefined;try{const id=Number(req.params.studentId);if(!Number.isSafeInteger(id)||id<=0||!req.file||!imageTypes.has(req.file.mimetype)||!ext(req.file.mimetype)||!signature(req.file)){res.status(400).json({error:'INVALID_PHOTO'});return}if(req.file.size>5*1024*1024){res.status(413).json({error:'PHOTO_TOO_LARGE'});return}const[rows]:any=await db.query(`SELECT photo_path FROM students WHERE id=? LIMIT 1`,[id]);if(!rows[0]){res.status(404).json({error:'STUDENT_NOT_FOUND'});return}const n=await save(PHOTO_DIR,req.file);saved=`photos/${n}`;await db.execute(`UPDATE students SET photo_path=? WHERE id=?`,[saved,id]);const old=rows[0].photo_path&&full(rows[0].photo_path);if(old)await fs.unlink(old).catch(()=>{});res.status(201).json({photo:true})}catch(e){if(saved){const p=full(saved);if(p)await fs.unlink(p).catch(()=>{})}next(e)}});storageRouter.post('/documents',requirePermission('documents.manage'),upload.single('file'),async(req,res,next)=>{let saved:string|undefined;try{if(!req.file||!documentTypes.has(req.file.mimetype)||!ext(req.file.mimetype)||!signature(req.file)){res.status(400).json({error:'INVALID_DOCUMENT'});return}const{category,title,description='',audience='ALL'}=req.body;if(!['MATERI','BUKU_KERJA_GURU','BUKU_SANTRI','PERATURAN_GURU','SILABUS','SOAL','LAINNYA'].includes(category)||!['ALL','SUPER_ADMIN','ADMIN','GURU','SANTRI'].includes(audience)||typeof title!=='string'||!title.trim()){res.status(400).json({error:'INVALID_DOCUMENT_METADATA'});return}const n=await save(DOC_DIR,req.file);saved=`documents/${n}`;const[r]:any=await db.execute(`INSERT INTO documents(category,title,description,file_path,mime_type,file_size,audience,published,uploaded_by) VALUES(?,?,?,?,?,?,?,0,?)`,[category,title.trim(),description,saved,req.file.mimetype,req.file.size,audience,req.auth!.userId]);res.status(201).json({id:r.insertId})}catch(e){if(saved){const p=full(saved);if(p)await fs.unlink(p).catch(()=>{})}next(e)}});storageRouter.get('/documents/:id/file',async(req,res,next)=>{try{const id=Number(req.params.id);if(!Number.isSafeInteger(id)||id<=0){res.status(400).json({error:'INVALID_DOCUMENT_ID'});return}const[rows]:any=await db.query(`SELECT file_path,mime_type,title,audience,published FROM documents WHERE id=? LIMIT 1`,[id]),d=rows[0];if(!d||!d.published){res.status(404).json({error:'DOCUMENT_NOT_FOUND'});return}const allowed=d.audience==='ALL'||d.audience===req.auth!.role||(req.auth!.role==='SUPER_ADMIN'&&d.audience==='ADMIN');if(!allowed){res.status(403).json({error:'DOCUMENT_FORBIDDEN'});return}const p=full(d.file_path);if(!p){res.status(500).json({error:'INVALID_STORAGE_PATH'});return}res.type(d.mime_type);res.setHeader('Content-Disposition',`inline; filename*=UTF-8''${encodeURIComponent(d.title+ext(d.mime_type))}`);res.sendFile(p,e=>{if(e&&!res.headersSent)res.status(sendFileStatus(e)).end()})}catch(e){next(e)}});storageRouter.get('/students/:studentId/photo',async(req,res,next)=>{try{const id=Number(req.params.studentId);if(!Number.isSafeInteger(id)||id<=0){res.status(400).end();return}let allowed=req.auth!.role==='ADMIN'||req.auth!.role==='SUPER_ADMIN'||req.auth!.studentId===id;if(req.auth!.role==='GURU'&&req.auth!.teacherId){const[s]:any=await db.query(`SELECT 1 FROM student_enrollments se WHERE se.student_id=? AND se.status='ACTIVE' AND (EXISTS(SELECT 1 FROM teaching_assignments ta WHERE ta.teacher_id=? AND ta.class_id=se.class_id AND ta.academic_year_id=se.academic_year_id AND ta.active=1) OR EXISTS(SELECT 1 FROM homeroom_assignments ha WHERE ha.teacher_id=? AND ha.class_id=se.class_id AND ha.academic_year_id=se.academic_year_id AND ha.active=1)) LIMIT 1`,[id,req.auth!.teacherId,req.auth!.teacherId]);allowed=!!s[0]}if(!allowed){res.status(403).end();return}const[rows]:any=await db.query(`SELECT photo_path FROM students WHERE id=? LIMIT 1`,[id]);if(!rows[0]?.photo_path){res.status(404).end();return}const p=full(rows[0].photo_path);if(!p){res.status(500).end();return}res.sendFile(p,e=>{if(e&&!res.headersSent)res.status(sendFileStatus(e)).end()})}catch(e){next(e)}});
